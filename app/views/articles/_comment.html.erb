<div class="comment">
    <h4><%= comment.author_name %></h4>
    <a class="timestamp"><%= distance_of_time_in_words(comment.article.created_at, comment.created_at) %> later</a>
    <p><%= comment.body %></p>
</div>
